#################################################################
#                                                               #
#                             Locks                             #
#                                                               #
#################################################################
script:
  frontdoor_manual_notify:
    mode: parallel
    sequence:
      - service: notify.admins
        data:
          title: "{{ title }}"
          message: "{{ message }}"

# sensor:
# Notifications appear to come from here, but not clear if I monitor these or not?
#
# homeassistant/Frontdoor/notification/Keypad_unlock_operation_userId
# homeassistant/Frontdoor/notification/Keypad_lock_operation_userId
# homeassistant/Frontdoor/notification/Manual_unlock_operation
# homeassistant/Frontdoor/notification/Manual_lock_operation
# homeassistant/Frontdoor/notification/RF_unlock_operation
# homeassistant/Frontdoor/notification/RF_lock_operation

#   - platform: template
#     sensors:
#       front_door_code:
#         friendly_name: Front Door Last Code
#         entity_id:
#           - sensor.front_door_lock_alarm_type
#           - sensor.front_door_lock_alarm_level
#         value_template: >-
#           {% if is_state('sensor.front_door_lock_alarm_type', '18') or
#                 is_state('sensor.front_door_lock_alarm_type', '19') %}
#             {% if not is_state('sensor.front_door_lock_alarm_level', '0') %}
#               {{states('sensor.front_door_lock_alarm_level')}}
#             {% else %}
#               {# just provide previous value if no user code entered #}
#               {{states('sensor.front_door_code')}}
#             {% endif %}
#           {% else %}
#             {# just provide previous value if no user code entered #}
#             {{states('sensor.front_door_code')}}
#           {% endif %}

# automation:

# - id: '1613229838552'
#   alias: Lock Status Change
#   description: ''
#   trigger:
#   - platform: mqtt
#     topic: homeassistant/Frontdoor/notification/Keypad_unlock_operation_userId
#   action:
#   - event: zwave.scene_change
#     event_data:
#       scene_id: '{{ trigger.payload_json.data[0].id|int }}'
#       scene: '{{ trigger.payload_json.data[1] }}'

#  # homeassistant/Frontdoor/notification

#   # - alias: Lock Status Change
#   #   trigger:
#   #       - platform: state
#   #         entity_id: lock.frontdoor_lock
#   #         from: 'unlocked'
#   #         to: 'locked'
#   #       - platform: state
#   #         entity_id: lock.frontdoor_lock
#   #         from: 'locked'
#   #         to: 'unlocked'
#   #   condition:
#   #     - condition: state
#   #       entity_id: person.mike
#   #       state: 'not_home'
#   #   action:
#   #     service: notify.admins
#   #     data:
#   #       title: "Front Lock"
#   #       message: >
#   #         {% set user_slot = states("sensor.front_door_lock_last_user_code") %}
#   #         {% if user_slot == "1" %}
#   #         {% set user = "Person1" %}
#   #         {% elif user_slot == "2" %}
#   #         {% set user = "Person2" %}
#   #         {% elif user_slot == "3" %}
#   #         {% set user = "Person3" %}
#   #         {% endif %}
#   #         {{ trigger.to_state.attributes.friendly_name }} is {{ trigger.to_state.state }}{% if user is defined %} by {{user}}{% endif %}
