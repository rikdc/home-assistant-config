---
name: Yaml Lint
on:
  push:
    branches-ignore:
      - main
      - gh-pages


  workflow_dispatch:

jobs:
  lintAllTheThings:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: yaml-lint
        uses: ibiqlik/action-yamllint@v3
